install node.js
run a command prompt from this directory and run the following commands:
1) npm install
2) node server.js

server can be accessed at localhost:3000

1) will install the required packages
2) will start the server